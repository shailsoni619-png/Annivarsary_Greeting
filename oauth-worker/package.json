{
  "name": "oauth-worker",
  "version": "1.0.0",
  "description": "This worker provides `/auth` and `/callback` for Decap CMS GitHub login when the app is hosted on GitHub Pages.",
  "main": "worker.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "wrangler": "^4.67.0"
  }
}
